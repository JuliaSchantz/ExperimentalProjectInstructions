{
  "name": "thrifty",
  "version": "1.0.0",
  "description": "This project is an online thrift store",
  "main": "src/index.html",
  "scripts": {
    "sync": "browser-sync start -s \"src\" -f \"src/**/*.*\"",
    "sass": "node-sass -w --include-path='src/scss' --source-map=true src/styles.scss src/styles.css",
    "start": "npm-run-all -p sync sass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rachel Ingram",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.7",
    "node-sass": "^4.9.3",
    "npm-run-all": "^4.1.3"
  },
  "dependencies": {
    "jquery": "^3.5.1"
  }
}
